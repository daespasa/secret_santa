<!DOCTYPE html>
<html lang="es" class="h-full bg-gray-100">
  <head>
    <%- include('./partials/head') %>
    <title>Ajustes de Perfil</title>
    <meta
      name="description"
      content="Gestiona tu perfil, foto, nombre y preferencias personales en Amigo Invisible."
    />
  </head>
  <body class="h-full flex flex-col">
    <%- include('./partials/header') %>

    <div class="flex-1 flex flex-col bg-gray-100 pt-16">
      <main class="flex-1 max-w-3xl mx-auto w-full px-4 sm:px-6 lg:px-8 py-8 sm:py-10">
        <%- include('./partials/flash') %>

        <div class="bg-white shadow rounded-lg p-6 mt-8">
            <h2 class="text-2xl font-bold mb-6">Ajustes de Perfil</h2>

            <!-- Profile Card -->
            <div class="mb-8 pb-8 border-b border-gray-200">
              <div class="flex items-center space-x-6">
                <div class="flex-shrink-0">
                  <% if (user.profilePhoto) { %>
                  <img
                    src="<%= user.profilePhoto %>"
                    alt="<%= user.name %>"
                    class="h-20 w-20 rounded-full object-cover"
                  />
                  <% } else { %>
                  <div
                    class="h-20 w-20 rounded-full bg-gradient-to-br from-red-400 to-red-600 flex items-center justify-center text-white text-3xl"
                  >
                    <%= user.name.charAt(0).toUpperCase() %>
                  </div>
                  <% } %>
                </div>
                <div>
                  <p class="text-sm text-gray-500">Foto de Perfil</p>
                  <p class="font-semibold text-gray-900"><%= user.name %></p>
                  <p class="text-sm text-gray-600"><%= user.email %></p>
                </div>
              </div>
            </div>

            <!-- Form -->
            <form
              method="post"
              action="/settings"
              enctype="multipart/form-data"
              class="space-y-6"
            >
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

              <div>
                <label class="block text-sm font-medium text-gray-700"
                  >Nombre</label
                >
                <input
                  type="text"
                  name="name"
                  value="<%= user.name %>"
                  required
                  class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 px-3 py-2 border"
                />
              </div>

              <div>
                <label class="block text-sm font-medium text-gray-700"
                  >Correo Electrónico</label
                >
                <input
                  type="email"
                  value="<%= user.email %>"
                  disabled
                  class="mt-1 block w-full rounded-md bg-gray-50 px-3 py-2 border border-gray-300 text-gray-600"
                />
                <p class="mt-1 text-xs text-gray-500">
                  El correo está vinculado a tu cuenta y no se puede cambiar.
                </p>
              </div>

              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2"
                  >Foto de Perfil</label
                >
                <div class="flex items-center gap-4">
                  <div class="flex-shrink-0">
                    <% if (user.profilePhoto) { %>
                    <img
                      src="<%= user.profilePhoto %>"
                      alt="<%= user.name %>"
                      class="h-16 w-16 rounded-full object-cover"
                    />
                    <% } else { %>
                    <div
                      class="h-16 w-16 rounded-full bg-gradient-to-br from-red-400 to-red-600 flex items-center justify-center text-white text-2xl"
                    >
                      <%= user.name.charAt(0).toUpperCase() %>
                    </div>
                    <% } %>
                  </div>
                  <div class="flex-grow">
                    <input
                      type="file"
                      name="profilePhoto"
                      accept="image/png,image/jpeg,image/webp"
                      class="block w-full text-sm text-gray-600 border border-gray-300 rounded-lg p-2 cursor-pointer file:mr-4 file:px-4 file:py-2 file:rounded-md file:bg-red-100 file:text-red-700 hover:file:bg-red-200"
                    />
                    <p class="mt-1 text-xs text-gray-500">
                      PNG, JPG o WebP. Máximo 5MB.
                    </p>
                  </div>
                </div>
              </div>

              <div class="flex justify-end gap-3 pt-4">
                <a
                  href="/dashboard"
                  class="inline-flex items-center px-4 py-2 rounded-md border border-gray-300 text-gray-700 bg-white hover:bg-gray-50"
                >
                  Cancelar
                </a>
                <button
                  type="submit"
                  class="inline-flex items-center px-4 py-2 rounded-md bg-red-600 text-white hover:bg-red-700"
                >
                  Guardar Cambios
                </button>
              </div>
            </form>
          </div>

          <% if (user.password) { %>
          <!-- Change Password Section -->
          <div class="bg-white shadow rounded-lg p-6 mt-6">
            <h3 class="text-xl font-bold mb-4 flex items-center gap-2">
              <%- renderIcon("lock", "text-xl") %> Cambiar Contraseña
            </h3>
            
            <form
              method="post"
              action="/settings/change-password"
              class="space-y-4"
            >
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

              <div>
                <label for="currentPassword" class="block text-sm font-medium text-gray-700 mb-1">
                  Contraseña actual
                </label>
                <input
                  type="password"
                  id="currentPassword"
                  name="currentPassword"
                  required
                  class="block w-full rounded-md border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 px-3 py-2 border"
                  placeholder="Tu contraseña actual"
                />
              </div>

              <div>
                <label for="newPassword" class="block text-sm font-medium text-gray-700 mb-1">
                  Nueva contraseña
                </label>
                <input
                  type="password"
                  id="newPassword"
                  name="newPassword"
                  required
                  class="block w-full rounded-md border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 px-3 py-2 border"
                  placeholder="Mínimo 6 caracteres"
                />
                <p class="mt-1 text-xs text-gray-500">
                  Mínimo 6 caracteres
                </p>
              </div>

              <div>
                <label for="confirmPassword" class="block text-sm font-medium text-gray-700 mb-1">
                  Confirmar nueva contraseña
                </label>
                <input
                  type="password"
                  id="confirmPassword"
                  name="confirmPassword"
                  required
                  class="block w-full rounded-md border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 px-3 py-2 border"
                  placeholder="Repite la nueva contraseña"
                />
              </div>

              <div class="flex justify-end pt-4">
                <button
                  type="submit"
                  class="inline-flex items-center gap-2 px-4 py-2 rounded-md bg-blue-600 text-white hover:bg-blue-700 transition"
                >
                  <%- renderIcon("check", "text-base") %> Cambiar Contraseña
                </button>
              </div>
            </form>
          </div>
          <% } %>

          <!-- Info Section -->
          <div class="mt-8 bg-blue-50 border-l-4 border-blue-400 p-4 rounded">
            <h3
              class="font-semibold text-blue-900 mb-2 flex items-center gap-2"
            >
              <%- renderIcon("lightbulb", "text-lg") %> Información
            </h3>
            <p class="text-sm text-blue-800">
              Tu perfil es privado. Otros usuarios solo ven tu nombre cuando les
              asignas un regalo.
            </p>
          </div>
        </div>
      </main>
    </div>
    <%- include('./partials/footer') %>
  </body>
</html>
