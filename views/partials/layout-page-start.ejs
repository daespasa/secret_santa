<!DOCTYPE html>
<html lang="es" class="h-full">
  <head>
    <%- include('./head') %>
    <title><%= typeof title !== 'undefined' ? title : 'Página - Amigo Invisible' %></title>
    <% if (typeof description !== 'undefined') { %>
    <meta name="description" content="<%= description %>" />
    <% } %>
  </head>
  <body class="h-full flex flex-col">
    <%- include('./header') %>
    <div class="flex-1 flex flex-col bg-gray-100 pt-16">
      <!-- Flash Messages -->
      <% if (flash && (flash.error || flash.success || flash.info)) { %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <%- include('./flash') %>
      </div>
      <% } %>
      <!-- Main Content -->
      <main class="flex-1 px-4 sm:px-6 lg:px-8 py-8 sm:py-10">
        <div class="max-w-7xl mx-auto">
