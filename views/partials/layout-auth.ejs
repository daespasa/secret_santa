<!-- Auth page layout - centered, minimal -->
<!DOCTYPE html>
<html lang="es" class="h-full">
  <head>
    <%- include('./head') %>
    <title>
      <%= typeof title !== 'undefined' ? title : 'Autenticación - Amigo
      Invisible' %>
    </title>
    <% if (typeof description !== 'undefined') { %>
    <meta name="description" content="<%= description %>" />
    <% } %>
  </head>
  <body class="h-full flex flex-col bg-gradient-to-br from-red-50 to-pink-50">
    <%- include('./header') %>

    <main
      class="flex-1 flex items-center justify-center px-4 sm:px-6 lg:px-8 py-8 sm:py-12"
    >
      <div class="w-full max-w-md"><%- body %></div>
    </main>

    <%- include('./footer') %>
  </body>
</html>
