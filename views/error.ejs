<!DOCTYPE html>
<html lang="es" class="h-full">
  <head>
    <%- include('./partials/head') %>
    <title>Error</title>
    <meta
      name="description"
      content="Ha ocurrido un error. Vuelve al inicio y sigue organizando tu amigo invisible."
    />
  </head>
  <body class="h-full flex flex-col bg-gray-50">
    <%- include('./partials/header') %>

    <main
      class="flex-1 max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-10 w-full pt-16"
    >
      <div class="bg-white border-l-4 border-red-500 shadow rounded p-6">
        <h1 class="text-2xl font-bold text-red-600 mb-2">Error</h1>
        <p class="text-gray-800 mb-6"><%= message %></p>
        <a
          href="/"
          class="inline-flex items-center px-4 py-2 rounded-md bg-red-600 text-white hover:bg-red-700"
          >Volver</a
        >
      </div>
    </main>
    <%- include('./partials/footer') %>
  </body>
</html>
