# =====================================
# AMIGO INVISIBLE - Variables de Entorno
# =====================================

# ===== APLICACIÓN =====
# URL base de la aplicación
BASE_URL=http://localhost:3000

# Clave secreta para sesiones (cambia en producción)
SESSION_SECRET=change-me-with-a-long-random-string

# Entorno (development, production)
NODE_ENV=development

# ===== AUTENTICACIÓN GOOGLE OAUTH 2.0 =====
# Obten estas credenciales de https://console.cloud.google.com
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# ===== BASE DE DATOS =====
# Para desarrollo y producción (SQLite en contenedor):
# Usa la ruta absoluta /data dentro del contenedor para que se persista en /DATA/secretsanta/data
DB_PATH=/data/app.db
DATABASE_URL=file:/data/app.db

# Para producción (PostgreSQL):
# DATABASE_URL=postgresql://user:password@localhost:5432/secret_santa

# ===== EMAIL (BREVO SMTP) =====
# Modo de email: dev (log en consola) | smtp (envío real)
EMAIL_MODE=dev

# Configuración SMTP (Brevo recomendado)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-brevo-smtp-password
EMAIL_FROM="Secret Santa <noreply@example.com>"

# ===== OPCIONALES =====
# Puerto del servidor (default: 3000)
# PORT=3000

# Log de peticiones HTTP (combined, short, dev)
# MORGAN_FORMAT=combined

# ===== CLOUDFLARE TUNNEL (DOMINIO) =====
# Token del túnel creado en Cloudflare Zero Trust → Tunnels → Create Tunnel → Run tunnel
# Configura en Cloudflare el Public Hostname: secretsanta.daespasa.com → http://app:3000
CLOUDFLARE_TUNNEL_TOKEN=

# ===== BASE URL (PRODUCCIÓN CON TÚNEL) =====
# Para producción, ajusta a tu dominio final
BASE_URL=https://secretsanta.daespasa.com
